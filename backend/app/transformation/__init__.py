"""Phase 6 — Transformation Engine.

Provides the LangGraph-driven transformation workflow that turns one source
into multiple independently-tracked outputs using shared canonical content
(Phase 4) and optional RAG context (Phase 5).
"""
